{
  "name": "gdrive-smart",
  "version": "0.1.0",
  "private": true,
  "description": "Smart MCP for Google Drive (files, folders, sharing), single-account, wraps ~/.santo-agent OAuth token jar.",
  "type": "module",
  "main": "dist/server.js",
  "exports": {
    ".": { "types": "./dist/server.d.ts", "import": "./dist/server.js" },
    "./client": { "types": "./dist/client.d.ts", "import": "./dist/client.js" }
  },
  "bin": {
    "gdrive-smart": "dist/server.js",
    "gdrive-smart-auth": "dist/bin/gdrive-smart-auth.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/server.js dist/bin/gdrive-smart-auth.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "smoke": "timeout 3 node dist/server.js < /dev/null"
  },
  "dependencies": {
    "smart-mcp-core": "*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "msw": "^2.6.0",
    "@types/node": "^22.0.0"
  }
}
