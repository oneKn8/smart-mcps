# calendar-smart

Smart MCP server for Google Calendar (read + write), single account, sharing the
`~/.santo-agent/oauth/` token jar with `email-smart`.

Status: scaffolding (Phase 5 task 1). Full tool surface and usage documentation
land in task 12.
