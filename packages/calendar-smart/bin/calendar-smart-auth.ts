#!/usr/bin/env node
// Stub — full OAuth flow lands in Phase 5 task 3. Mints
// `~/.santo-agent/oauth/<account>.calendar.json` with the calendar scope,
// reusing `~/.santo-agent/oauth/client.json` for the OAuth client id/secret.
// TODO(task-3): implement runAuth({ account, home, codeReader }) and wire main.
throw new Error(
  "calendar-smart-auth: not yet implemented (lands in Phase 5 task 3)",
);
